# Implementation Plans

Generated by the improve skill on 2026-07-25 against commit `06c3362`. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 002 | Fix apply/preview state correctness | P1 | M | — | DONE |
| 003 | History undo + Cancel during apply | P1 | S–M | 002 | DONE |
| 004 | Path trust + drop shell + settings allowlist | P1 | M | — (before any convert work) | DONE |
| 001 | Rename-shell polish pack (theme/fonts/a11y) | P1 | M | Prefer after 002; may parallel carefully | DONE |
| 005 | Docs honesty + session hard cap | P2 | S | — (anytime; cheap) | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Recommended sequence

1. **002** first — stale paths, numbering bug, preview races, and invoke-based job completion must be solid before Cancel/Undo UX.
2. **003** next — History undo and Cancel need accurate `activeJobId` / post-rename file identity from 002.
3. **004** in parallel with 002/003 or immediately after — harden apply/preview path trust and remove unused shell before any convert/metadata backends.
4. **001** after or carefully alongside 002 — UI polish only; avoid fighting unfinished Apply state. Do not wait on convert.
5. **005** anytime — docs + hard-cap; low risk; do early if agents will keep working in this repo.

## Dependency notes

- **003 depends on 002**: Cancel needs `START_JOB` with a real `job_id`; post-rename Undo/history must not re-apply against stale `original_path` values.
- **004 before future convert**: convert pipelines will mutate paths; trust client `FileInfo` less first.
- **001 does not implement SettingsModal full Task 5** (output dir / retention / parallel jobs) — that is deferred direction; 001 only makes appearance + rename shell meet the polish bar.
- **005 does not implement convert/metadata** — it marks them deferred honestly and fixes the 5k session cap.

## Findings considered and rejected (do not re-plan)

- Convert/Metadata “not implemented” as a **bug** — by design for v0.1.0 (README); 001 only improves presentation (hide stubs).
- Full ffmpeg/audio/video convert — conflicts with 10MB / stack lock; direction only.
- Transitive `cargo audit` advisories on `quick-xml` / `crossbeam-epoch` — not on user rename payloads; track upstream.
- Mid-batch crash durability (DB write after all renames) — real finding; **deferred** from A–E selection; plan separately if selected later.
- Windows CI / auto-updater — out of A–E scope.
